/*
// src/store/types.ts
import type { store } from './store'; // только тип, без значения!
import { rootReducer } from './rootReducer';

export type RootState   = ReturnType<typeof rootReducer>;
export type AppDispatch = typeof store.dispatch;*/
